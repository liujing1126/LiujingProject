How to get SIM number:
    TelephonyManager telephonyManager = (TelephonyManager)mcontext.getSystemService(mcontext.TELEPHONY_SERVICE);
        String myNumber = telephonyManager.getLine1Number();
        android.util.Log.d("lj","SomcCallBarringEditPreference:myNumber= "+myNumber);
